version: '3.8'

services:
  test_client:
    build:
      context: ../../
      dockerfile: tests/clients/Dockerfile
    networks:
      - front-net



networks:
  front-net:
    name: docker_front-net
    external: true
